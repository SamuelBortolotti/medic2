<form method="post" action="{{ path('app_rendez_vous_controller_crud_delete', {'patient_id': rendez_vou.patient_id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rendez_vou.patient_id) }}">
    <button class="btn">Delete</button>
</form>
